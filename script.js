window.onload = function () {
	
	// Initialize.
    init();
    
};

init = function() {
	
	// Dimensions.
	var width = 1000;
	var height = 1000;
	
	// Initialize Crafty.
    Crafty.init(width, height);
    Crafty.canvas.init();
    
};
